import React from 'react';
import StyledSpinner from '../styled/StyledSpinner';

const Spinner = () => {
  return (
    <StyledSpinner>
      <h2>Hold on... We are searching for your movie</h2>
      <div></div>
    </StyledSpinner>
  );
};

export default Spinner;
