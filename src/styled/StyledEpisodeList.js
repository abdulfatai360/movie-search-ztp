import styled from 'styled-components';

const StyledEpisodeList = styled.section`
  margin-top: 50px;

  h2 {
    font-size: 1em;
    font-family: 'ibm_plex_sansmedium', sans-serif;
    text-transform: uppercase;
  }

  ul {
    border-top: 1px solid #dfe1e5;
    display: flex;
    flex-wrap: wrap;
  }
`;

export default StyledEpisodeList;
